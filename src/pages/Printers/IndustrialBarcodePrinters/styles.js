import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {flex: 1},
  imageStyle: {
    margin:5,
    height: 180,
    width: '100%',
    resizeMode: 'contain',
  },
});
export default styles;
